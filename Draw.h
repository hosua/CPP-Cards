class DrawHandler{
	// Draw some number of cards, with a some number of them displayed face down.
	void drawCards(vector<Card> cardVect, int numCards=5, int faceDown=0);S
	// I don't even know if I need this but whatever
	void drawSingle(Card c);
}
