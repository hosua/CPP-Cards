#include "Draw.h"

class War{
	public:
		Card getVictor(Card a, Card b){
			Card c;
			int aVal = rankVal.find(a.getRank())->second;
			int bVal = rankVal.find(b.getRank())->second;
			return c;
		}

};

